"""
Unit test of with, match, else blocks.

`with`: context manager protocol
- __enter__, __exit__
- contextlib: @contextmanager, AbstractContextManager, ContextDecorator, ExitStack

`match/case`: pattern match

`else` clause in `for`, `while`, `try` statements

"""
# pylint: skip-file


import unittest
